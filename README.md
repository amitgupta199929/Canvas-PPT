I have completed the required Nessus configuration on the Linux VMs that I currently have access to.

From the list you shared, only three VMs are hosted on the VMware environment. I have successfully configured two of them. For the remaining VMware VM, I am currently receiving Access Denied, so I am unable to complete the configuration.

The rest of the Linux VMs appear to be hosted on Azure, and I do not currently have access to them. I will need the required access (or an approved access request) before I can connect to those servers and apply the requested configuration.

Could you please advise on how I should proceed? If needed, I can raise an access request for the Azure-hosted VMs.
